export const DEBOUNCE_DELAY_TIME = 500;

export const NUMBER_REPOSITORIES_TO_FETCH = 5;

export const END_REACHED_THRESHOLD = 0.5; // must be between 0 and 1

export const NUMBER_REVIEWS_TO_FETCH = 5; // use for both MyReviews and single repository page